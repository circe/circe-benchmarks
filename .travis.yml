language: scala
sudo: required
dist: trusty

scala:
  - 2.12.8
  - 2.13.0

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot

install:
  - pip install --user codecov

script:
  - sbt ++$TRAVIS_SCALA_VERSION coverage test scalastyle coverageReport && codecov
